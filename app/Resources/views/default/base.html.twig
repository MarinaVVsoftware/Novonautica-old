<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ asset('bower_components/bootstrap/dist/css/bootstrap.min.css') }}">
    <style>
        body {
            padding-top: 50px;
        }

        .brand {
            padding: 5px;
            max-width: 11rem;
        }

        .novo-checkbox-container .radio {
            display: inline-block;
            position: relative;
            padding: 10px 20px 10px 20px;
            text-align: left;
        }

        .novo-checkbox-container .radio label {
            cursor: pointer;
            padding-left: 10px;
        }

        .novo-checkbox-container .radio label:before,
        .novo-checkbox-container .radio label:after {
            content: '';
            position: absolute;
            top: 50%;
        }

        .novo-checkbox-container .radio label:before {
            left: 0;
            width: 20px;
            height: 20px;
            margin: -10px 0 0;
            border-radius: 4px;
            background: #3e4766;
            -webkit-box-shadow: 0 0 1px grey;
            box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.5), 0 1px 0 rgba(255, 255, 255, 0.4);
        }

        .novo-checkbox-container .radio label:after {
            width: 14px;
            height: 8px;
            top: 14px;
            left: 3px;
            border: 3px solid #0097f6;
            border-top: none;
            border-right: none;
            background: transparent;
            opacity: 0;
            -webkit-transform: rotate(-45deg);
            transform: rotate(-45deg);
        }

        .novo-checkbox-container .radio label:hover:after {
            opacity: .5;
        }

        .novo-checkbox-container .radio input[type="radio"],
        .novo-checkbox-container .radio input[type="checkbox"] {
            position: absolute;
            top: 0;
            left: -9999px;
            visibility: hidden;
        }

        .novo-checkbox-container .radio input[type="radio"]:checked + label:after,
        .novo-checkbox-container .radio input[type="checkbox"]:checked + label:after {
            opacity: 1;
        }

        .btn {
            border-radius: 0;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top" style="background-color: #292e41">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="brand">
                <img src="{{ asset('img/logo.png') }}" alt="Novonautica" class="img-responsive">
            </div>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a href="{{ path('clients_logout') }}">Salir</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

{% block body %}
    <div class="container">

        <div class="starter-template">
            <h1>Bootstrap starter template</h1>
            <p class="lead">Use this document as a way to quickly start any new project.<br> All you get is this text
                and a mostly barebones HTML document.</p>
        </div>

    </div>
{% endblock body %}

<script src="{{ asset('bower_components/jquery/dist/jquery.min.js') }}"></script>
<script src="{{ asset('bower_components/bootstrap/dist/js/bootstrap.min.js') }}"></script>
{% block javascripts %}
{% endblock javascripts %}
</body>
</html>