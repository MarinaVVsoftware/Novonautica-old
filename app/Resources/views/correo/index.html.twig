{% extends 'estructura/plantilla.twig' %}

{% block contwrapper %}
<div class="content-wrapper">
    <section class="content">
        <div class="panel panel-novo">
            <div class="panel-heading">
                <h3 class="panel-title">Historial de correos</h3>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-sm-8">
                        <table class="table">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>Fecha</th>
                                <th>Tipo</th>
                                <th>Descripción</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% set i=1 %}
                            {% for correo in correos %}
                                <tr>
                                    <td>{{ i }}</td>
                                    <td>{% if correo.fecha %}{{ correo.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                                    <td>{% if correo.tipo == 1 %}
                                            Cotización Servicio Marina Humeda
                                        {% elseif correo.tipo == 2%}
                                            Recotización Servicio Marina Humeda
                                        {% elseif correo.tipo == 3%}
                                            Cootización Servicio Astillero
                                        {% endif %}
                                    </td>
                                    <td>{{ correo.descripcion }}</td>
                                </tr>
                                {% set i = i+1 %}
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
        </div>
    </section>
</div>
{% endblock %}
