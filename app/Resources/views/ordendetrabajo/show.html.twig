{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ordendetrabajo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordenDeTrabajo.id }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ ordenDeTrabajo.observaciones }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ordendetrabajo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ordendetrabajo_edit', { 'id': ordenDeTrabajo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
