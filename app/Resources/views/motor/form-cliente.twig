{% block form_widget %}
    {% spaceless %}
        <div class="col-sm-4">
            <div class="form-group">
                {{ form_row(form.marca) }}
                {{ form_row(form.modelo) }}
                <a href="#" class="remove-motor btn btn-xs btn-naranja">
                    <i class="fa fa-times" aria-hidden="true"></i>
                    Remover Motor
                </a>
            </div>
        </div>
    {% endspaceless %}
{% endblock form_widget %}