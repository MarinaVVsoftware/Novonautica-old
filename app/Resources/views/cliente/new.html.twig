{% extends 'estructura/plantilla.twig' %}

{% block title %}
    Novonautiva | Agregar Cliente
{% endblock %}

{% block contwrapper %}
    <div class="content-wrapper">
        <section class="content">
            <div class="panel panel-novo">
                <div class="panel-heading">
                    <h3 class="panel-title">Nuevo Cliente</h3>
                </div>
                <div class="panel-body">
                    {{ form_start(form) }}
                    <div class="row">
                        <div class="col-sm-5">
                            {{ form_row(form.nombre) }}
                            {{ form_row(form.correo) }}
                            {{ form_row(form.telefono) }}
                            {{ form_row(form.celular) }}
                            {{ form_row(form.direccion) }}
                        </div>
                        <div class="col-sm-5">
                            {{ form_row(form.empresa) }}
                            {{ form_row(form.razonsocial) }}
                            {{ form_row(form.rfc) }}
                            {{ form_row(form.direccionfiscal) }}
                            {{ form_row(form.correofacturacion) }}

                        </div>
                    </div>
                    <div class="row">
                        <div class="col-sm-12 espacio-linea">
                            <hr>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-sm-5">
                            {{ form_label(form.barcos.children[0].nombre) }}
                            {{ form_widget(form.barcos.children[0].nombre) }}

                            {{ form_row(form.barcos.children[0].modelo) }}
                            {{ form_row(form.barcos.children[0].eslora) }}
                            {{ form_row(form.barcos.children[0].calado) }}
                            {{ form_row(form.barcos.children[0].manga) }}
                        </div>
                        <div class="col-sm-5">
                            {{ form_row(form.barcos.children[0].nombreResponsable) }}
                            {{ form_row(form.barcos.children[0].telefonoResponsable) }}
                            {{ form_row(form.barcos.children[0].correoResponsable) }}
                            {{ form_row(form.barcos.children[0].nombreCapitan) }}
                            {{ form_row(form.barcos.children[0].telefonoCapitan) }}
                            {{ form_row(form.barcos.children[0].correoCapitan) }}
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-sm-12 espacio-linea">
                            <hr>
                        </div>
                    </div>
                    <div id="motor-fields-list0"
                         class="lista-motores"
                         data-prototype="
                                    {% filter escape %}
                                    {{ include('motor/form-cliente.twig', { 'form': form.barcos.children[0].motores.vars.prototype }) }}
                                        {#{{ form_widget(form.barcos.children[0].motores.vars.prototype)|e('html_attr') }}#}
                                    {% endfilter %}
                                        ">
                        {% for motorField in form.barcos.children[0].motores %}
                            {% form_theme motorField 'motor/form-cliente.twig' %}
                            <div class="row">
                                {{ form_widget(motorField) }}
                            </div>
                        {% endfor %}
                    </div>
                    <div class="row">
                        <div class="col-sm-12">
                            <a href="#" id="" class="add-another-motor letra-azul"
                               data-cantidad="{{ form.barcos.children[0].motores|length }}" data-idlista="0">
                                + Agregar Motor
                            </a>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-sm-12">
                            <button type="submit" class="btn btn-azul pull-right">
                                <i class="fa fa-floppy-o" aria-hidden="true"></i>
                                Guardar
                            </button>
                        </div>
                    </div>
                    {{ form_end(form) }}
                    <div class="row espacio-linea-arriba2">
                        <div class="col-sm-12">
                            <a href="{{ path('cliente_index') }}" class="btn btn-naranja pull-right">
                                <i class="fa fa-ban" aria-hidden="true"></i>
                                Cancelar
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
