{% extends 'estructura/plantilla.twig' %}

{% block title %}
{% endblock %}

{% block contwrapper %}
<div class="content-wrapper">
    <section class="content">
        <div class="panel panel-novo">
            <div class="panel-heading">
                Cuentas de Banco
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-sm-8">
                        <table class="table">
                            <thead>
                            <tr>
                                <th>Id</th>
                                <th>Banco</th>
                                <th>Clabe</th>
                                <th></th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for cuentaBancarium in cuentaBancarias %}
                                <tr>
                                    <td><a href="{{ path('cuenta-bancaria_show', { 'id': cuentaBancarium.id }) }}">{{ cuentaBancarium.id }}</a></td>
                                    <td>{{ cuentaBancarium.banco }}</td>
                                    <td>{{ cuentaBancarium.clabe }}</td>
                                    <td>
                                        <ul>
                                            <li>
                                                <a href="{{ path('cuenta-bancaria_show', { 'id': cuentaBancarium.id }) }}">show</a>
                                            </li>
                                            <li>
                                                <a href="{{ path('cuenta-bancaria_edit', { 'id': cuentaBancarium.id }) }}">edit</a>
                                            </li>
                                        </ul>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
            <div class="panel-footer">
                <a href="{{ path('cuenta-bancaria_new') }}" class="btn btn-azul">
                    <i class="fa fa-plus" aria-hidden="true"></i>
                    Agregar Cuenta
                </a>
            </div>
        </div>
    </section>
</div>
{% endblock %}
