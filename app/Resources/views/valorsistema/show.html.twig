{% extends 'base.html.twig' %}

{% block body %}
    <h1>Valorsistema</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ valorSistema.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ valorSistema.nombre }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ valorSistema.valor }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ajustes_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ajustes_edit', { 'id': valorSistema.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
